\chapter*{不等式}
\subsection*{不等式的性质}
\begin{enumerate}
    \item (2024$\cdot$杨浦区二模$\cdot$14)已知实数 $a$ 、 $b$ 、 $c$ 、 $d$ 满足 $a > b > 0 > c > d$ ，则下列不等式一定正确的是\dotfill{(\kh{C})}
    \xx{$a + d > b + c$}{${ad} > {bc}$}{$a + c > b + d$}{${ac} > {bd}$}

    \item (2024$\cdot$崇明区二模$\cdot$13)若 $a > b$ ， $c < 0$ ，则下列不等式成立的是 \dotfill{(\kh{D})}
    \xx{$a > b - c$}{$\dfrac{a}{c} > \dfrac{b}{c}$}{$a + c < b + c$}{$a{c}^{2} > b{c}^{2}$}

    \item (2025$\cdot$嘉定区二模$\cdot$13)已知实数 $a$ 、 $b$ 满足 $a > b$ ，则下列不等式中，不恒成立的是 \dotfill{(\kh{B})}
    \xx{${a}^{3} > {b}^{3}$}{$\dfrac{a}{b} > 1$}{${a}^{2} + {b}^{2} > {2ab}$}{${2}^{a} > {2}^{b}$}

    \item (2024$\cdot$浦东新区一模$\cdot$13)如果 $a > 0 > b$ ，则下列不等式中一定成立的是 \dotfill{(\kh{D})}
    \xx{$\sqrt{a} > \sqrt{-b}$}{${a}^{2} > {b}^{2}$}{${a}^{2} < {ab}$}{${a}^{3} > {b}^{3}$}
\end{enumerate}

\subsection*{解分式不等式}
\begin{enumerate}
    \item 不等式 $\dfrac{5 - x}{{x}^{2} - {2x} - 3} \leq   - 1$ 的解集为\blkda[4]{$\left( {-1,1\rbrack \cup \lbrack 2,3}\right)$}.
    \begin{jieda}
        $\dfrac{5 - x}{{x}^{2} - {2x} - 3} \leq   - 1 \Longleftrightarrow \dfrac{{x}^{2} - {3x} + 2}{{x}^{2} - {2x} - 3} \leq  0 \Longleftrightarrow\left\{  \begin{array}{l} \left( {x + 1}\right) \left( {x - 1}\right) \left( {x - 2}\right) \left( {x - 3}\right)  \leq  0 \\  x \neq   - 1\text{ 且 }x \neq  3 \end{array}\right.$

        穿针引线可得解集为$\left( {-1,1\rbrack \cup \lbrack 2,3}\right)$ 
    \end{jieda} 

    \item 关于 $x$ 的不等式 $\dfrac{\left( {x + 3}\right) {\left( x - 2\right) }^{999}{\left( x + 1\right) }^{2}}{{\left( x - 1\right) }^{2015}{\left( x - 4\right) }^{2016}} \leq  0$ 的解集为\blkda[4]{$(- \infty , - 3 \rbrack  \cup  \{  - 1\}  \cup  (1,2\rbrack$}
    \begin{jieda}
        原不等式等价于: $\left\{  \begin{array}{l} \left( {x + 3}\right) {\left( x - 2\right) }^{999}{\left( x + 1\right) }^{2}{\left( x - 1\right) }^{2015}{\left( x - 4\right) }^{2016} \leq  0, \\  x - 1 \neq  0, \\  x - 4 \neq  0, \end{array}\right. $
        
        结合高次不等式奇穿偶不穿的性质求解不等式组可得原不等式的解集为: $(- \infty , - 3 \rbrack  \cup  \{  - 1\}  \cup  (1,2\rbrack$
    \end{jieda} 
    \item 解关于$x$的不等式：$\dfrac{a}{x-2} > 1-a$
    \begin{jieda}
        原不等式化为$\dfrac{(a-1)x+(2-a)}{x-2} > 0$，进一步化为$[(a-1)x+(2-a)](x-2) > 0$ 
        \begin{dingautolist}{172}
            \item $a > 1$时，不等式等价于$\left[x-\dfrac{a-2}{a-1}\right](x-2) > 0$，又因为$\dfrac{a-2}{a-1} = 1 - \dfrac{1}{a-1} < 1$，所以原不等式的解集为$\left(-\infty, \dfrac{a-2}{a-1}\right) \cup (2, +\infty)$
            \item $a = 1$时，不等式等价于$x-2 > 0$，解集为$(2, +\infty)$
            \item $a < 1$时，不等式等价于$\left[x-\dfrac{a-2}{a-1}\right](x-2) < 0$ \\
            此时$\dfrac{a-2}{a-1} < 2 \Longleftrightarrow a < 0$，因此：\\
            $a < 0$时原不等式解集为$\left(\dfrac{a-2}{a-1}, 2\right)$ \\
            $a = 0$时原不等式解集为$\varnothing$ \\
            $a \in (0, 1)$时原不等式解集为$\left(2, \dfrac{a-2}{a-1}\right)$
        \end{dingautolist}
        \ps 参数会影响二次方程的两个根的大小关系，而最高次项含参则会涉及到退化回一次函数，以及二次函数开口方向.
    \end{jieda}

    \item 已知集合 $A = \left\{  {x\left| {\dfrac{{ax} - 2}{x - a} \leq  0}\right. }\right\}$ ,若 $2 \notin  A$ ,则实数 $a$ 的取值范围是\blkda[2]{$(1,2\rbrack$}.
    \begin{jieda}
        集合 $A = \left\{  {x\left| {\dfrac{{ax} - 2}{x - a} \leq  0}\right. }\right\}$ ,若 $2 \notin  A$ ,故 $\dfrac{{2a} - 2}{2 - a} > 0$ 或 $2 - a = 0$ ,解得 $1 < a \leq  2$ .
    \end{jieda}
\end{enumerate}

\subsection*{已知不等式解集求参数}
\begin{enumerate}
    \item 不等式 $a{x}^{2} + {bx} + 2 > 0$ 的解集是 $\left\{  {x\left| { - \dfrac{1}{2} < x < \dfrac{1}{3}}\right. }\right\}$ ,则 $a + b =$ \blkda[2]{$-14$}.
    \begin{jieda}
        不等式 $a{x}^{2} + {bx} + 2 > 0$ 的解集是 $\left\{  {x\left| { - \dfrac{1}{2} < x < \dfrac{1}{3}}\right. }\right\} \Longleftrightarrow \left\{  \begin{array}{l} a < 0 \\   - \dfrac{b}{a} =  - \dfrac{1}{6}, \\  \dfrac{2}{a} =  - \dfrac{1}{6} \end{array}\right.$ 
        
        解得$\left\{  {\begin{array}{l} a =  - {12} \\  b =  - 2 \end{array}}\right.$，故$a + b =  - {14}$
    \end{jieda}
    \item 若关于 $x$ 的不等式 $a{x}^{2} + {bx} + c < 0$ 的解集是 $\left( {-\infty , - 1}\right)  \cup  \left( {3, + \infty }\right)$ ,则关于 $x$ 的不等式$a{x}^{2} + {cx} + {2b} \geq  0$ 的解集是\blkda[2]{$\left\lbrack  {-1,4}\right\rbrack$}.
    \begin{jieda}
        不等式 $a{x}^{2} + {bx} + c < 0$ 的解集是 $\left( {-\infty , - 1}\right)  \cup  \left( {3, + \infty }\right) \Longleftrightarrow \left\{  \begin{array}{l}  
        a < 0 \\
        a-b+c = 0 \\  
        9+3b+c =  0 
        \end{array}\right.$
        
        解得 $\left\{\begin{array}{l}
             a < 0 \\
             b =  - {2a}  \\
             c =  - {3a} 
        \end{array}\right.$，故$a{x}^{2} + {cx} + {2b} \geq  0 \Longleftrightarrow a{x}^{2} - {3ax} - {4a} \geq  0$
        
        因 $a < 0$ ,故得 ${x}^{2} - {3x} - 4 \leq  0$ ,解得 $x \in \left\lbrack  {-1,4}\right\rbrack$
    \end{jieda}
    \item ${ax} + b < 0$ 的解集为 $\left( {-\infty , - 1}\right)$ ,则 $\left( {{ax} - b}\right) \left( {x + 2}\right)  < 0$ 的解集为\blkda[2]{$(-2,1)$}.
    \begin{jieda}
         ${ax} + b < 0$ 的解集为 $\left( {-\infty , - 1}\right)$，即$\left\{\begin{array}{l}
              b = a  \\
              a > 0
         \end{array}\right.$

         故$\left( {{ax} - a}\right) \left( {x + 2}\right)  < 0 \Longrightarrow  \left( {x - 1}\right) \left( {x + 2}\right)  < 0$，解得$- 2 < x < 1$ 
    \end{jieda}
\end{enumerate}

\subsection*{整根问题}
\begin{enumerate}
    \item 关于 $x$ 的不等式 ${x}^{2} - \left( {1 + {2a}}\right) x + {2a} < 0$ 的解集中恰有 2 个整数,则实数 $a$ 的取值范围是\blkda[4]{$\left\lbrack  {-1, - \dfrac{1}{2}}\right)  \cup  \left( {\dfrac{3}{2},2}\right\rbrack$}.
    \begin{jieda}
        ${x}^{2} - \left( {1 + {2a}}\right) x + {2a} < 0 \Longleftrightarrow \left( {x - 1}\right) \left( {x - {2a}}\right)  < 0$ ,

        \begin{dingautolist}{172}
            \item 当 ${2a} = 1$ 时,不等式化为 ${\left( x - 1\right) }^{2} < 0$ ,此时无解
            \item 当 ${2a} > 1$ 时,解得 $1 < x < {2a}$ ,要使解集中恰有两个整数,则 $3 < {2a} \leq  4$ ,得 $\dfrac{3}{2} < a \leq  2$
            \item 当 ${2a} < 1$ 时,解得 ${2a} < x < 1$ ,要使解集中恰有两个整数,则 $- 2 \leq  {2a} <  - 1$ ,得 $- 1 \leq  a <  - \dfrac{1}{2}$
        \end{dingautolist}
        综上，$a \in \left\lbrack  {-1, - \dfrac{1}{2}}\right)  \cup  \left( {\dfrac{3}{2},2}\right\rbrack$ .
    \end{jieda}
    \item 已知关于 $x$ 的不等式组 $\left\{  \begin{array}{l} {x}^{2} - {2x} - 8 > 0, \\  2{x}^{2} + \left( {{2k} + 7}\right) x + {7k} < 0 \end{array}\right.$ 仅有一个整数解,则实数 $k$ 的取值范围是\blkda[2]{$ [-5, 3) \cup (4,5]$}.
    \begin{jieda}
        不等式 ${x}^{2} - {2x} - 8 > 0$ 的解集为 $\{ x \mid  x <  - 2$ 或 $x > 4\}$ .
        
        不等式 $2{x}^{2} + \left( {{2k} + 7}\right) x + {7k} < 0$ 化为 $\left( {x + k}\right) \left( {x + \dfrac{7}{2}}\right)  < 0$ .

        \begin{dingautolist}{172}
            \item 当 $k = \dfrac{7}{2}$ 时,不等式组的解集为 $\varnothing$ ,不符合题意.
            
            \item 当 $k > \dfrac{7}{2}$ 时,不等式 $2{x}^{2} + \left( {{2k} + 7}\right) x + {7k} < 0$ 的解集为 $\left\{x\left |  { - k < x <  - \dfrac{7}{2}}\right. \right\}$，由解集中的整数为$-4$,得 $- 5 \leq   - k <  - 4$ ,解得 $4 < k \leq  5$ .

            \item 当 $k < \dfrac{7}{2}$ 时,不等式 $2{x}^{2} + \left( {{2k} + 7}\right) x + {7k} < 0$ 的解集为 $\left\{  {x\left| { - \dfrac{7}{2} < x <  - k}\right. }\right\}$，由解集中的整数为$-3$,得 $- 3 <  - k \leq  5$ ,解得 $- 5 \leq  k < 3$ .
        \end{dingautolist}
        综上，$k \in [-5, 3) \cup (4,5]$.
    \end{jieda}

    \item 已知关于$x$的不等式组$\left \{ \begin{array}{l}
         \dfrac{x-a}{x-1+a} < 0\\
         x+3a>1 
    \end{array}\right.$的整数解恰有两个，则实数$a$的取值范围是\blkda[4]{$(1, 2]$}
    \begin{jieda}
        首先可以确定第一个不等式的解集为$(a, 1-a)$或$(1-a, a)$，因为$a$和$1-a$关于$\dfrac{1}{2}$对称，因此$\dfrac{1}{2}$必然在该不等式的解集中。而第二个不等式的解集为$(1-3a, +\infty)$
        \begin{dingautolist}{172}
            \item 若$a \in [0, 1]$，则$(a, 1-a)$或$(1-a, a)$没有整数解，故整个不等式组的解集中也没有整数解.
            \item 若$a < 0$，则$a < 0 < 1 < 1-a < 1-3a$，则整个不等式组解集为空集.
            \item 若$a > 1$，则$1-3a < 1-a < 0 < 1 < a$整个不等式组的解集为$(1-a, a)$，此时解集中必有两个整数解$0, 1$，故当$a \in (1, 2]$时，满足要求.
        \end{dingautolist}
        综上，$a \in (1, 2]$ \\
        \ps 本题画数轴可以更方便分析，本题判断出$a$和$1-a$关于$\dfrac{1}{2}$对称后可以猜测到两个整根是0和1.
    \end{jieda}
\end{enumerate}

\subsection*{解含绝对值不等式}
\begin{enumerate}
    \item 不等式 $\left| {1 - {4x}}\right|  > {2x}$ 的解集为\blkda[4]{$\left(-\infty, \dfrac{1}{6}\right) \cup \left(\dfrac{1}{2}, +\infty\right)$} 
    \begin{jieda}
        $\left| {1 - {4x}}\right|  > {2x} \Longleftrightarrow 1 - {4x} > {2x}$ 或 $1 - {4x} <  - {2x}$ , 解得 $x < \dfrac{1}{6}$ 或 $x > \dfrac{1}{2}$
    \end{jieda}
    \item 不等式 $\left| {{x}^{2} - 4}\right|  < x + 2$ 的解集为\blkda[2]{$(1, 3)$}. 
    \begin{jieda}
        $\left| {{x}^{2} - 4}\right|  < x + 2 \Longleftrightarrow -(x+2) < x^2-4 < (x+2) \Longleftrightarrow \left\{\begin{array}{l}
             (x+2)(x-3) < 0  \\
             (x+2)(x-1) > 0
        \end{array}\right.$，解得$ x \in (1,3)$
    \end{jieda}

    \item 不等式 $\left| {{2x} + 1}\right|  - \left| {5 - x}\right|  > 0$ 的解集为\blkda[4]{$\left( {-\infty , - 6}\right)  \cup  \left( {\dfrac{4}{3}, + \infty }\right)$}
    \begin{jieda}
        由不等式 $\left| {{2x} + 1}\right|  - \left| {5 - x}\right|  > 0$，可得 ${\left( 2x + 1\right) }^{2} > {\left( 5 - x\right) }^{2}$，即 $\left( {x + 6}\right) \left( {{3x} - 4}\right)  > 0$
        
        解得 $x \in  \left( {-\infty , - 6}\right)  \cup  \left( {\dfrac{4}{3}, + \infty }\right)$ .
    \end{jieda}
\end{enumerate}

\subsection*{根据单调性解不等式}
\begin{enumerate}
    \item (2024$\cdot$静安区一模$\cdot$10)不等式 ${\log }_{2}x + \dfrac{x}{2} < 4$ 的解集为\blkda[2]{$\left( {0,4}\right)$}.
    
    \item (2025$\cdot$长宁区一模$\cdot$9)已知 $\alpha  : {2}^{x} + {\log }_{2}x \leq  2,\beta  : x < m$ ，若 $\alpha$ 是 $\beta$ 的充分条件，则实数 $m$ 的取值范围是\blkda[2]{$(1, +\infty)$}.
    \begin{jieda}
        $y = {2}^{x} + {\log }_{2}x$严增，$\alpha : x \in (-\infty, 1]$，$(-\infty, 1] \subseteq (-\infty, m)$，故$m > 1$.
    \end{jieda}

    \item 不等式 ${27}^{x} + 7{\log }_{5}\left( {{36x} + 1}\right)  < {23}$ 的解集为\blkda{$\left( {-\dfrac{1}{36},\dfrac{2}{3}}\right)$}.
\end{enumerate}

\subsection*{平均值不等式（基础）}

\begin{table}[h]
    \centering
    \begin{tabular}{|c|c|c|c|}
        \hline
        名称 & 平均值不等式 & 重要不等式 & 常用不等式 \\
        \hline
        符号表述 & $\dfrac{a+b}{2} \geq \sqrt{ab}$ & $a^2+b^2 \geq 2ab$ & $\left(\dfrac{a+b}{2}\right)^2 \geq ab$ \\
        \hline
        适用条件 & $a \geq 0, b \geq 0$ & $a, b \in \mathbf{R}$ & $a, b \in \mathbf{R}$ \\
        \hline
        等号成立条件 & $a = b$ & $a = b$ & $a = b$ \\
        \hline
    \end{tabular}
\end{table}

\begin{enumerate}
    \item 若 $x > 1$ ,则 $x + \dfrac{1}{x - 1}$ 的最小值是\blkda[2]{3}.
    \begin{jieda}
        $x + \dfrac{1}{x - 1} = x - 1 + \dfrac{1}{x - 1} + 1 \geq  2\sqrt{\left( {x - 1}\right)  \times  \dfrac{1}{x - 1}} + 1 = 3$ ,
        
        当且仅当 $x - 1 = \dfrac{1}{x - 1}$ 即 $x = 2$ 时取等号,
        
        $\therefore x = 2$ 时 $x + \dfrac{1}{x - 1}$ 取得最小值 3 .
    \end{jieda}
    
    \item 若 $x > 1$ ,则 $x + \dfrac{x}{x - 1}$ 的最小值是\blkda[2]{4}.
    \begin{jieda}
        $x + \dfrac{x}{x - 1} = x + \dfrac{1}{x - 1} + 1 = x - 1 + \dfrac{1}{x - 1} + 2 \geq  2\sqrt{\left( {x - 1}\right)  \times  \dfrac{1}{x - 1}} + 2 = 4$ ,
        
        当且仅当 $x - 1 = \dfrac{1}{x - 1}$ 即 $x = 2$ 时取等号,
        
        $\therefore x = 2$ 时 $x + \dfrac{1}{x - 1}$ 取得最小值 4.
    \end{jieda}
    \item (2024$\cdot$浦东新区二模$\cdot$14)已知 $a \in  \mathbf{R}$ ，则下列结论不恒成立的是 \dotfill{(\kh{B})}

    \xx{$a\left( {1 - a}\right)  \leq  \dfrac{1}{4}$}{$a + \dfrac{1}{a} \geq  2$}{$\left| {a - 1}\right|  + \left| {a + 2}\right|  \geq  3$}{$\sin a + \dfrac{1}{2 + \sin a} \geq  0$}

    \item 下列各式中, 最小值为4的是\dotfill(\kh{B})
    \xx
    {$x+2\sqrt{x}+5$}
    {$\dfrac{x^2+5}{\sqrt{x^2+1}}$}
    {$x^2+3+\dfrac{4}{x^2+3}$}
    {$x+\dfrac{4}{x}$}
    \begin{jieda}
        注意取等条件是否可以取到！
    \end{jieda}

    \item (2024$\cdot$徐汇区一模$\cdot$4)若实数 $x$ 、 $y$ 满足 $x + y = 2$ ，则 ${x}^{2} + {y}^{2}$ 的最小值为\blkda[2]{2}.
    \begin{jieda}
        ${x}^{2} + {y}^{2} = {\left( x + y\right) }^{2} - 2xy = 4 - 2xy \geq 4 - (x^2+y^2) \Longrightarrow  {x}^{2} + {y}^{2} \geq  2$ .
    \end{jieda}

    \item 若正实数 $a$ 、 $b$ 满足 $a^2+b^2 = ab + 1$，求$a+b$最大值.
    \begin{jieda}
        $a^2+b^2 = ab + 1 \Longleftrightarrow (a+b)^2 = 3ab + 1 \leq 3\left(\dfrac{a+b}{2}\right)^2 + 1$，故$\dfrac{(a+b)^2}{4} \leq 1$，$a+b \leq 2$（$a = b = 1$时取等）
    \end{jieda}

    \item 已知$x>0, y>0, x+3y+xy=9$，则$x+3y$的最小值为\blkda{6}
    \begin{jieda}
        $x = \dfrac{9-3y}{1+y}$，故$x+3y = \dfrac{9-3y}{1+y} + 3y = 3y - 3 + \dfrac{12}{1+y} = 3(1+y) + \dfrac{12}{1+y} - 6 \\
        \geq 2\sqrt{3(1+y) \times \dfrac{12}{1+y}} - 6 = 6$
    \end{jieda}
    \item (2025$\cdot$长宁区一模$\cdot$10)若正实数 $a$ 、 $b$ 满足 ${ab} = {2a} + b$ ，则 $a + {2b}$ 的最小值是\blkda[2]{9}.
    \begin{jieda}
        $\dfrac{1}{a} + \dfrac{2}{b} = 1$，$a+2b = (a+2b)\left(\dfrac{1}{a} + \dfrac{2}{b}\right) = 5 + \dfrac{2b}{a} + \dfrac{2a}{b} \geq 9$，等号成立时 $a = 3,b = 3$.
    \end{jieda}

    \item (2024$\cdot$徐汇区二模$\cdot$4)若正数 $a$ 、 $b$ 满足 $\dfrac{1}{a} + \dfrac{1}{b} = 1$ ，则 ${2a} + b$ 的最小值为\blkda[2]{$3 + 2\sqrt{2}$}.
    \begin{jieda}
        由已知 ${2a} + b = \left( {{2a} + b}\right) \left( {\dfrac{1}{a} + \dfrac{1}{b}}\right)  = 3 + \dfrac{2a}{b} + \dfrac{b}{a} \geq 3 + 2\sqrt{2}$ ,当且仅当 $\dfrac{2a}{b} = \dfrac{b}{a}$ ,即 $a = 1 + \dfrac{\sqrt{2}}{2},b = 1 + \sqrt{2}$ 时等号成立，故所求最小值是 $3 + 2\sqrt{2}$.
    \end{jieda}


    \item 已知 $a > 0,b > 0$ ,且 $\dfrac{1}{a + 1} + \dfrac{2}{b + 1} = 1$ ,则 $a + b$ 的最小值为\blkda[2]{$2\sqrt{2} + 1$}.
    \begin{jieda}
        由 $a > 0,b > 0,\dfrac{1}{a + 1} + \dfrac{2}{b + 1} = 1$，得 $a + b = \left( {a + 1}\right)  + \left( {b + 1}\right)  - 2 = \left( {\dfrac{1}{a + 1} + \dfrac{2}{b + 1}}\right) \left\lbrack  {\left( {a + 1}\right)  + \left( {b + 1}\right) }\right\rbrack   - 2 = \dfrac{b + 1}{a + 1} + \dfrac{2\left( {a + 1}\right) }{b + 1} + 1 \geq  2\sqrt{\dfrac{b + 1}{a + 1} \cdot  \dfrac{2\left( {a + 1}\right) }{b + 1}} + 1 = 2\sqrt{2} + 1$
        
        当且仅当 $\dfrac{b + 1}{a + 1} = \dfrac{2\left( {a + 1}\right) }{b + 1}$ ,即 $a = \sqrt{2},b = \sqrt{2} + 1$ 时, $a + b$ 取得最小值 $2\sqrt{2} + 1$ .
    \end{jieda}
\end{enumerate}
\subsection*{平均值不等式（提高）}
$H_n \leq G_n \leq A_n \leq Q_n$，其中：
\begin{enumerate}
    \item $H_n = \dfrac{n}{\sum_{i=1}^{n}\dfrac{1}{x_i}} = \dfrac{n}{\dfrac{1}{x_1} + \dfrac{1}{x_2} + ... + \dfrac{1}{x_n}}$
    \item $G_n = \sqrt[n]{\prod_{i=1}^{n}x_i} = \sqrt[n]{x_1 \cdot x_2 \cdot ... \cdot x_n}$
    \item $A_n = \dfrac{\sum_{i=1}^{n}x_i}{n} = \dfrac{x_1 + x_2 + ... + x_n}{n}$
    \item $Q_n = \sqrt{\dfrac{\sum_{i=1}^{n}x_i^2}{n}} = \sqrt{\dfrac{x_1^2 + x_2^2 + ... + x_n^2}{n}}$
\end{enumerate}

特别地，二维的情况为：$\dfrac{2}{\dfrac{1}{a} + \dfrac{1}{b}} \leq \sqrt{ab} \leq \dfrac{a+b}{2} \leq \sqrt{\dfrac{a^2 + b^2}{2}}$ \\
\par 三维的情况为：$\dfrac{3}{\dfrac{1}{a} + \dfrac{1}{b} + \dfrac{1}{c}} \leq \sqrt[3]{abc} \leq \dfrac{a+b+c}{3} \leq \sqrt{\dfrac{a^2 + b^2 + c^2}{3}}$

柯西不等式(二维形式)：$(a^2+b^2)\cdot(c^2+d^2) \geq (ac+bd)^2$，取等条件为$ad = bc$ 
\begin{enumerate}
    \item (2025$\cdot$松江区一模$\cdot$13)已知 $a > b > 0$ ，以下四个数中最大的是 \dotfill{(\kh{D})}

    \xx{$b$}{$\sqrt{ab}$}{$\dfrac{a + b}{2}$}{$\sqrt{\dfrac{{a}^{2} + {b}^{2}}{2}}$}

    \item 求函数$y = \sqrt{1+x} + \sqrt{2-2x}$的最大值.
    \begin{jieda}
        $a = 1, c = \sqrt{1+x}, b = \sqrt{2}, d = \sqrt{1-x}$

        $(ac + bd)^2 \leq (1+2) \cdot (1+x+1-x) = 6$，$ac+bd \leq \sqrt{6}$
    \end{jieda}
    \item  (2026$\cdot$金山区一模$\cdot$16)记曲线 $C : \dfrac{{\left| x\right| }^{n}}{a} + \dfrac{{\left| y\right| }^{n}}{b} = 1\left( {a > 0\text{且}b > 0,n > 0\text{且}n \in  \mathbf{R}}\right)$ 为 ${C}_{\left( a,b\right) }^{n}$ ,称其为 “超椭圆”. 命题\ding{172} : 直线 $l : {2x} + {9y} - {12} = 0$ 与超椭圆 ${C}_{\left( 3,2\right) }^{\frac{1}{2}}$ 有 3 个不同交点; 命题\ding{173} : 超椭圆 ${C}_{\left( 9,1\right) }^{8}$ 上的点到坐标原点距离的取值范围为 $\left\lbrack  {1,{2}^{\frac{5}{8}}}\right\rbrack$ . 则下列说法正确的是\dotfill{(\kh{B})}
    \xx{\ding{172}  \ding{173} 都正确}{\ding{172} 正确， \ding{173} 错误}{\ding{172} 错误， \ding{173} 正确}{\ding{172}  \ding{173} 错误}
    \begin{jieda}
        \begin{dingautolist}{172}
            \item 画图，分象限讨论
            \begin{center}
                \includegraphics[width=0.4\textwidth]{bodyxb/src/26jinshanyimo16.png}
            \end{center}
            \item ${C}_{\left( 9,1\right) }^{8} : \dfrac{{x}^2}{9} + {y}^{8} = 1$ ,由图形的对称性,不妨考虑 $x \geq  0,y \geq  0$ .
            
            即 ${\left( \dfrac{{x}^{4}}{3}\right) }^{2} + {\left( {y}^{4}\right) }^{2} = 1$ ,令 $\left\{  \begin{array}{l} \dfrac{{x}^{4}}{3} = \cos \alpha \\  {y}^{4} = \sin \alpha  \end{array}\right.$ ,则 $\left\{  \begin{array}{l} {x}^{2} = \sqrt{3\cos \alpha } \\  {y}^{2} = \sqrt{\sin \alpha } \end{array}\right.$
            
            ${x}^{2} + {y}^{2} = \sqrt{3\cos \alpha } + \sqrt{\sin \alpha } \leq \sqrt{(3+1) \cdot (sin\alpha + cos\alpha)} \leq \sqrt{4\sqrt{2}}$，但两个等号不能同时取，故为假.
        \end{dingautolist}

    \end{jieda}
\end{enumerate}

\subsection*{三角不等式}
\begin{enumerate}
    \item (2025$\cdot$杨浦区一模$\cdot$5)已知 $\left| {x + 3}\right|  + \left| {x - 5}\right|  = 8$ ，则实数 $x$ 的取值范围为\blkda[2]{$\left\lbrack  {-3,5}\right\rbrack$}.
    \item (2025$\cdot$上海春季卷$\cdot$8)关于 $x$ 的方程 $\left| {x - 1}\right|  + \left| {\pi  - x}\right|  = \pi  - 1$ 的解集为\blkda[2]{$\left\lbrack  {1,\pi }\right\rbrack$}.
    \item (2025$\cdot$杨浦区二模$\cdot$8)不等式 $\left| {x + 3}\right|  + \left| {a - x}\right|  > 6$ 对一切实数 $x$ 恒成立，则实数 $a$ 的取值范围为\blkda[3.5]{$\left( {-\infty , - 9}\right)  \cup  \left( {3, + \infty }\right)$}.
    \item (2024$\cdot$静安区一模$\cdot$8)若不等式 $\left| {x - 3}\right|  + \left| {x - 5}\right|  \geq  a$ 对所有实数 $x$ 恒成立，则实数 $a$ 的取值范围是\blkda[2]{$( - \infty ,2\rbrack$}.
    \item 若关于$x$的不等式$\abs{x-1}-\abs{x-2}<a$的解集为$\varnothing$, 则实数$a$的取值范围为\blkda{$a\leq-1$}. 
    \item 设函数 $f\left( x\right)  = 5 - \left| {x + a}\right|  - \left| {x - 2}\right|$ .
    \begin{enumerate}
        \item 当 $a = 1$ 时,求不等式 $f\left( x\right)  \geq  0$ 的解集
        \item 若 $f\left( x\right)  \leq  1$ 恒成立,求 $a$ 的取值范围
    \end{enumerate}
    \begin{jieda}
        \begin{enumerate}
            \item 当 $a = 1$ 时，$f\left( x\right)  = \left\{  \begin{array}{ll} 
            {2x} + 4, & x \leq   - 1 \\
            2, & - 1 < x \leq  2 \\
            - {2x} + 6, &x > 2. \end{array}\right.$
            
            可得 $f\left( x\right)  \geq  0$ 的解集为 $\{ x \mid   - 2 \leq  x \leq  3\}$ .
            \item $f\left( x\right)  \leq  1$ 等价于 $\left| {x + a}\right|  + \left| {x - 2}\right|  \geq  4$ .
            
            而 $\left| {x + a}\right|  + \left| {x - 2}\right|  \geq  \left| {a + 2}\right|$ ,且当 $x = 2$ 时等号成立. 故 $f\left( x\right)  \leq  1$ 等价于 $\left| {a + 2}\right|  \geq  4$ .
            
            由 $\left| {a + 2}\right|  \geq  4$ 可得 $a \leq   - 6$ 或 $a \geq  2$ ,所以 $a$ 的取值范围是 $\left( {-\infty , - 6}\right\rbrack   \cup  \lbrack 2, + \infty )$ .
        \end{enumerate}
    \end{jieda}
\end{enumerate}

\subsection*{恒成立问题}
\begin{enumerate}
    \item 若存在 $x \in  \left\lbrack  {1,3}\right\rbrack$ ,使不等式 ${x}^{2} - {2ax} + a + 2 \leq  0$ 成立,则 $a$ 的取值范围为\blkda[4]{$\lbrack 2, + \infty )$}.
    \begin{jieda}
        由 ${x}^{2} - {2ax} + a + 2 \leq  0 \Longrightarrow  {x}^{2} + 2 \leq  a\left( {{2x} - 1}\right)$

        因为 $x \in  \left\lbrack  {1,3}\right\rbrack$ ,所以 ${2x} - 1 \in  \left\lbrack  {1,5}\right\rbrack$ ,令 $t = {2x} - 1 \in  \left\lbrack  {1,5}\right\rbrack  ,x = \dfrac{t + 1}{2}$ ,
        
        由 ${x}^{2} + 2 \leq  a\left( {{2x} - 1}\right)  \Longrightarrow  a \geq  \dfrac{{x}^{2} + 2}{{2x} - 1} = \dfrac{\dfrac{1}{4}\left( {{t}^{2} + {2t} + 9}\right) }{t} = \dfrac{1}{4}\left( {t + \dfrac{9}{t} + 2}\right)$ ,
        
        构造函数 $g\left( t\right)  = \dfrac{1}{4}\left( {t + \dfrac{9}{t} + 2}\right)  \geq  \dfrac{1}{4}\left( {2\sqrt{t \cdot  \dfrac{9}{t}} + 2}\right)  = 2$ ,
        
        即 $g{\left( t\right) }_{\min } = 2$ ,当且仅当 $t = 3 \in  \left\lbrack  {1,5}\right\rbrack$ 时取等号，所以 $a \geq  g{\left( t\right) }_{\min } = 2$
    \end{jieda} 
    \item (2024$\cdot$长宁区一模$\cdot$10) 若 “存在 $x > 0$ ，使得 ${x}^{2} + {ax} + 1 < 0$”是假命题，则实数 $a$ 的取值范围为\blkda[2]{$\lbrack  - 2, + \infty )$}.
    \begin{jieda}
        由题意可得: “任意 $x > 0$ ,使得 ${x}^{2} + {ax} + 1 \geq  0$ ” 是真命题，等价于 “任意 $x > 0$ ,使得 $x + \dfrac{1}{x} \geq   - a$ ” 是真命题,因为 $x + \dfrac{1}{x} \geq  2\sqrt{x \cdot  \dfrac{1}{x}} = 2$ ,当且仅当 $x = \dfrac{1}{x}$ ,即 $x = 1$ 时,等号成立,所以 $2 \geq   - a$，所以实数 $a$ 的取值范围 $\lbrack  - 2, +\infty )$.
    \end{jieda}

    \item 已知函数 $f\left( x\right)  = e^x - 2ax \geq  0$在$(0, +\infty)$恒成立,则实数 $a$ 的取值范围是\blkda{$\left(-\infty, \dfrac{e}{2}\right]$}
    \begin{jieda}
        \begin{dingautolist}{172}
            \item 直曲分离：$y = 2ax$恒在$y = e^x$下方，相切为边界，$y = e^x$过原点的切线为$y = ex$，故$2a \in (0, e]$，因此$a \in \left(-\infty, \dfrac{e}{2}\right]$
            \item 参变分离：$2a \leq \dfrac{e^x}{x}$，设$g(x) = \dfrac{e^x}{x}$，$g'(x) = \dfrac{e^x(x-1)}{x^2}$，故$g(x)$在$(0, 1)$严减，在$(1, +\infty)$严增，故$2a \leq e$，因此$a \in \left(-\infty, \dfrac{e}{2}\right]$
            \item 含参求导：$f''(x) = e^x - 2a$
            
            若$a \leq 0$，则$f'(x)$严增，$f'(x) \geq f'(0) = 1$满足要求

            若$a > 0$，则$f'(x)$在$(0, ln(2a))$严减，在$(ln(2a), +\infty)$严增，故$f'(x) \geq f'(ln(2a)) = 2a - 2aln(2a) \geq 0$，故$a \leq \dfrac{e}{2}$

            综上，$a \in \left(-\infty, \dfrac{e}{2}\right]$
        \end{dingautolist}

        \ps 题干可以改成“已知函数 $f\left( x\right)  = e^x - ax^2$在$(0, +\infty)$单调递增”.
    \end{jieda}

    \item (2024$\cdot$虹口区二模$\cdot$12)已知关于 $x$ 的不等式 $\left( {\ln x - {kx}}\right) \left\lbrack  {{x}^{2} - \left( {k + 3}\right) x + 4}\right\rbrack   \leq  0$ 对任意 $x \in  \left( {0, + \infty }\right)$ 均成立，则实数 $k$ 的取值范围为\blkda[2]{$\left\lbrack  {\dfrac{1}{{e}},1}\right\rbrack$}.
    \begin{jieda}
        $\left( {\ln x - {kx}}\right) \left\lbrack  {{x}^{2} - \left( {k + 3}\right) x + 4}\right\rbrack   \leq  0$ 对任意 $x \in  (0$ , $+ \infty )$ 均成立
        
        即$\left( {\dfrac{\ln x}{x} - k}\right) \left\lbrack  {x + \dfrac{4}{x} - \left( {k + 3}\right) }\right\rbrack   \leq  0$在$(0, +\infty)$恒成立
        
        即$ \left( {k - \dfrac{\ln x}{x}}\right)  \left\lbrack  {k - \left( {x + \dfrac{4}{x} - 3}\right) }\right\rbrack   \leq  0$在$(0, +\infty)$恒成立
        
        即$\dfrac{\ln x}{x} \leq  k \leq  x + \dfrac{4}{x} - 3$在$(0, +\infty)$恒成立
        
        ${y}_{1} = \dfrac{\ln x}{x} \leq  \dfrac{1}{{e}},{y}_{2} = x + \dfrac{4}{x} - 3 \geq  2\sqrt{x \times  \dfrac{4}{x}} - 3 = 1$ , 所以实数 $k$ 的取值范围为 $k \in  \left\lbrack  {\dfrac{1}{{e}},1}\right\rbrack$ .

        \begin{center}
        \includegraphics[width=0.2\textwidth]{bodyxb/src/12_423_1184_221_274_0.jpg}
        \end{center}
    \end{jieda}
\end{enumerate}